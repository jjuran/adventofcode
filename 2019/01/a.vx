#!/usr/bin/env vx

const bindir = dirname realpath argv[ 0 ]

const input_path = if argc > 1 then {argv[ 1 ]} else {bindir "/input.txt"}

const result = (load input_path).lines() map { int v div 3 - 2 } per {a + b}

print result
